dependencies:
  override:
    - npm install
  post: 
    - "sudo pip install awscli==1.7.43"
deployment: 
  production: 
    branch: master
    commands: 
      - ". ./scripts/deploy.sh"
      - ". ./scripts/deploy_site.sh"
experimental: 
  notify: 
    branches: 
      only: 
        - master
